# 体重管理模块验证记录

最近验证：2026-07-04

## 已验证

- 前端执行 `npm run build` 成功，Vue TypeScript 检查和 Vite 双入口构建通过。
- 后端使用 Java 21 执行 `mvn clean package -DskipTests` 成功。
- 应用连接本机 MySQL 启动成功，自动创建 `weight_profile`、`weight_record` 并加载 7 条初始记录。
- 免登录、无 token 公开接口返回：昵称“最硬”、183.2 斤、BMI 27.7、体脂率 24.3%、坚持 40 天、进度 60%、7 条趋势数据。
- 后台接口已验证登录、个人档案读取、记录列表、新增记录和删除记录；冒烟数据已删除。
- 未填写体脂率的临时记录自动估算为 24.7%，并返回 `fatEstimated=true`；临时记录已删除。
- 独立 H5 `/weight.html` 返回 200，ECharts 正常渲染。
- 浏览器以 375×812 视口验证：页面宽度、文档 scrollWidth 均为 375px，无横向滚动；昵称、头像、数据及图表正常，控制台无错误。
- PC 视口验证页面主体最大宽度为 480px并居中。
- 多人接口验证：创建第二位女性人员后，人员列表返回 2 人，体重记录按 `profile_id` 完全隔离。
- 批量登记验证：从 2026-06-01 到 2026-06-29、每 7 天一次，正确生成并新增 5 条记录。
- 重复批量提交验证：关闭覆盖时跳过 5 条，开启覆盖时更新 5 条，没有重复日期。
- 女性体脂估算验证：身高 165cm、最新体重 139 斤时估算体脂 32.3%，返回 `fatEstimated=true`。
- 手机端 `/weight.html?id=人员ID` 能正确展示指定人员；不传 ID 时仍展示第一位有效人员。
- 浏览器验证多人选择、男女标识、批量登记弹窗、7/15天快捷间隔和自定义间隔输入均正常，控制台无错误。
- 斤/kg 切换验证：起始体重 208.6斤切换后显示 104.3kg，目标体重 150斤显示 75.0kg。
- 在 kg 模式保存个人信息后，接口读取数据库仍为 208.60斤和150.00斤，确认入库统一使用斤。
- 每个人均可单独编辑昵称并上传 JPG、PNG、WebP 头像，头像地址按人员档案保存。
- 批量登记弹窗内直接提供斤/kg选项；切换到kg后表头立即变为“体重（kg）”，提交仍换算为斤。
- 开发环境 `/uploads` 代理验证返回图片 200、`image/png`，解决 Vite 页面上传后头像不显示。
- 独立头像保存接口验证通过：第3人头像地址更新成功，图片读取 200；前端上传后自动保存并刷新预览。

## 说明

- 体脂率优先展示后台录入的实测值；未录入时按 BMI、记录日期年龄和性别估算，并标记“估算”。
- 删除人员采用停用方式，历史记录保留，默认不再出现在人员列表和手机展示页。
